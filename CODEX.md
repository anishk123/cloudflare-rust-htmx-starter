# Codex

`AGENTS.md` is authoritative. Read `AGENTS.md` and `README.md` before editing.

Follow `CONTRIBUTING.md` for the shared contribution loop. Start with `cargo xtask check`; develop locally with `cargo xtask dev`; finish with `cargo xtask verify`. Do not deploy unless explicitly requested.
