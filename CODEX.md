# Codex

`AGENTS.md` is authoritative. Read `AGENTS.md` and `README.md` before editing.

Start with `cargo xtask check`; develop locally with `cargo xtask dev`; finish with `cargo xtask verify`. Do not deploy unless explicitly requested.
