# Security policy

Do not open public issues containing credentials or exploitable private-environment details. Rotate any accidentally exposed secret immediately; deleting a Git commit is not secret rotation. For starter-level vulnerabilities, use GitHub private vulnerability reporting when enabled by the repository owner.
