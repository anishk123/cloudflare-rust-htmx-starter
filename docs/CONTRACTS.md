# Contracts

Cross-process/durable shapes belong in `crates/contracts` and derive Serde + Schemars. Version Queue/offline/event contracts. `/contracts` exports JSON Schema. Update producers, consumers and tests together. Do not duplicate ad-hoc request/Queue structs inside route handlers.
